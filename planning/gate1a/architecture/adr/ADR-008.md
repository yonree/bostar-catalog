# ADR-008：服务端组件与客户端组件边界

- status: `PROPOSED`

## context

当前页面、数据整形和 UI 逻辑耦合。

## decision

数据获取与 SEO 默认在服务端，交互表单、媒体选择器等保留客户端。

## alternatives

全部客户端；全部服务端。

## consequences

降低首屏 JS，提升可抓取性。

## risks

边界切分不当会导致 props 过大。

## rollback

交互重的区块可局部回退为客户端组件。
